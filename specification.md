Potrebno definisati strukturu sesije, pitati da li treba koristiti session
management bazu.
Primer atributa sesije: SessionID, IPAdress, User-Agent, email, username, user ID,
role, language preferences, current state, last login, session timeouts, TTL

CacheStats hitCount, missCount, loadSuccessCount, LoadErrCount,
totalLoadTime, evictionCount
